{
  "name": "serverless-terraform-outputs",
  "version": "0.0.0",
  "private": true,
  "description": "Serverless plugin to reference outputs from Terraform",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "checkTypes": "tsc --noEmit",
    "//": "matching tests inside the nested folder requires workaround - see https://github.com/nodejs/help/issues/3902#issuecomment-1809746747",
    "test": "tsx --test ./src/*.test.ts"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "prettier": "@sureshraj/prettier-config",
  "devDependencies": {
    "@sureshraj/prettier-config": "^1.0.0",
    "@types/node": "^20.14.8",
    "prettier": "^3.3.2",
    "tsx": "^4.15.7",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "serverless": ">=3"
  }
}
